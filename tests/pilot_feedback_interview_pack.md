# 试用反馈访谈包

用途：收集真实、脱敏、可追溯的试用反馈，支撑创新性和应用价值证据。不得伪造用户反馈，不得把估算写成已实现收益。

角色数量：6

## R01 生产主管

推荐用例：T01 / T36 / T38 / T39 / S01 / S10

价值关注：跨部门待办 / 同步节奏 / 生产影响 / 闭环门禁

访谈问题：

1. 这类现场异常平时由谁整理，平均一次需要多久？
2. Skill 输出的统一事件包是否能直接作为生产部同步依据？
3. 哪些待办、截止时间或验收标准最有价值？

记录要求：

- 只记录角色和场景，不记录个人姓名、手机号、客户名称、订单号或系统账号。
- 没有实际计时数据时，`before_minutes`、`after_minutes` 和 `time_saved_minutes` 必须留空或标为待验证。
- 没有授权时，`consent_to_public` 填 `no`，公开短评留空或只写匿名脱敏摘要。

## R02 设备维修

推荐用例：T01 / T25 / T34 / T35 / S07

价值关注：候选原因 / 备件工具 / 工时 / 试运行 / 关闭前数据质量

访谈问题：

1. Skill 是否补齐了派工前常漏的设备、现象、备件或工时字段？
2. 候选诊断矩阵是否有助于安排下一步验证？
3. 关闭门禁是否能减少低质量维修记录？

记录要求：

- 只记录角色和场景，不记录个人姓名、手机号、客户名称、订单号或系统账号。
- 没有实际计时数据时，`before_minutes`、`after_minutes` 和 `time_saved_minutes` 必须留空或标为待验证。
- 没有授权时，`consent_to_public` 填 `no`，公开短评留空或只写匿名脱敏摘要。

## R03 质量工程师

推荐用例：T06 / T21 / T30 / T33 / S02 / S08

价值关注：隔离范围 / 复检结论 / 放行授权 / 冲突事实 / CAPA

访谈问题：

1. Skill 是否清楚区分事实、推断和待确认质量结论？
2. 是否避免替质量负责人做放行判断？
3. QMS 动作卡是否覆盖复检、隔离、处置和授权字段？

记录要求：

- 只记录角色和场景，不记录个人姓名、手机号、客户名称、订单号或系统账号。
- 没有实际计时数据时，`before_minutes`、`after_minutes` 和 `time_saved_minutes` 必须留空或标为待验证。
- 没有授权时，`consent_to_public` 填 `no`，公开短评留空或只写匿名脱敏摘要。

## R04 EHS/安全

推荐用例：T11 / T12 / T38 / ST05 / ST12 / S04 / S06

价值关注：危险作业拒绝 / 许可 / 能量隔离 / 整改验收

访谈问题：

1. Skill 是否拒绝了危险操作步骤并保留安全边界？
2. EHS 许可、隔离和整改验收字段是否完整？
3. 输出是否适合形成安全整改或宣导记录？

记录要求：

- 只记录角色和场景，不记录个人姓名、手机号、客户名称、订单号或系统账号。
- 没有实际计时数据时，`before_minutes`、`after_minutes` 和 `time_saved_minutes` 必须留空或标为待验证。
- 没有授权时，`consent_to_public` 填 `no`，公开短评留空或只写匿名脱敏摘要。

## R05 PMC/计划

推荐用例：T36 / T38 / T39 / S05 / S10

价值关注：产能 / 交期 / 加班 / 插单 / 客户承诺边界

访谈问题：

1. 生产影响和交期风险是否足够清晰？
2. 是否能看到质量、物料和维修状态对排产的约束？
3. 下一次同步时间和升级路径是否可执行？

记录要求：

- 只记录角色和场景，不记录个人姓名、手机号、客户名称、订单号或系统账号。
- 没有实际计时数据时，`before_minutes`、`after_minutes` 和 `time_saved_minutes` 必须留空或标为待验证。
- 没有授权时，`consent_to_public` 填 `no`，公开短评留空或只写匿名脱敏摘要。

## R06 数字化负责人

推荐用例：T21 / T28 / T39 / ST08 / ST09

价值关注：系统动作卡 / 幂等键 / 回执检查 / 降级恢复

访谈问题：

1. 动作卡字段是否足够映射到 MES/CMMS/QMS/EHS/SAP/ERP/OA/PMC？
2. 准备度门禁是否能避免误写外部系统完成状态？
3. 工具失败时的降级完成和恢复路径是否清楚？

记录要求：

- 只记录角色和场景，不记录个人姓名、手机号、客户名称、订单号或系统账号。
- 没有实际计时数据时，`before_minutes`、`after_minutes` 和 `time_saved_minutes` 必须留空或标为待验证。
- 没有授权时，`consent_to_public` 填 `no`，公开短评留空或只写匿名脱敏摘要。
